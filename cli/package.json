{
  "name": "@kurochenko/lore",
  "version": "0.2.0",
  "description": "Drop-in knowledge base that tells your LLM what's true about your project before it writes code.",
  "type": "module",
  "bin": {
    "lore": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "build:test": "tsc -p tsconfig.test.json",
    "dev": "tsc --watch",
    "test": "bun run build:test && node --test dist/commands/__tests__/*.test.js"
  },
  "dependencies": {
    "commander": "^13.0.0",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/anomalyco/living-spec"
  }
}
